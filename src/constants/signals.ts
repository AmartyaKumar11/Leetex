/** No code change or LeetCode interaction for this duration → idle. */
export const IDLE_TIMEOUT_MS = 120_000

/** Fire MAJOR_REWRITE when similarity falls below this (0–1). */
export const MAJOR_REWRITE_SIMILARITY_THRESHOLD = 0.4

export const SIGNAL_POLL_INTERVAL_MS = 1_000

export const RESULT_POLL_INTERVAL_MS = 500

export const RESULT_POLL_MAX_ATTEMPTS = 40

export const FIRST_EDIT_MIN_CODE_LENGTH = 3
