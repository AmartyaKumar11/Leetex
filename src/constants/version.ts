/**
 * User-facing / export version label.
 * Chrome manifest uses package.json "0.3.0" (integers only — no -alpha suffix).
 */
export const LEETEX_VERSION = "0.3.1.1-alpha"

export const LEETEX_MANIFEST_VERSION = "0.3.1"

export const LEETEX_PLATFORM = "leetcode" as const
